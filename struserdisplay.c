//Program in c to input a string of any length
// from the user and display the same 
#include <stdio.h>

int main() {
    char arr[100];
    printf("enter the string");
    scanf("%s", &arr);
    printf("the str is");
    printf("%s", arr);
    }